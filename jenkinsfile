pipeline {
    agent any
    
    tools {
        maven 'maven'
        jdk 'jdk'
    }
    
    stages {
        stage('Build') {
            steps {
                bat 'mvn -B -DskipTests clean package'
            }
        }
        stage('Test') {
            steps {
               bat 'mvn test'
            }
        }
        stage('Deliver') {
            steps {
                bat 'chmod +x scripts/deliver.sh'
                bat './scripts/deliver.sh'
            }
        }
    }
}